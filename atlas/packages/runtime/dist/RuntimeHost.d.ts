import type { Application, ApplicationHost, EventBus, ServiceContainer } from "@atlas/kernel";
import type { AsyncDisposable, DiagnosticReport, LifecycleState } from "@atlas/foundation";
import type { RuntimeHealthReport } from "./RuntimeHealthReport";
import type { RuntimeHostConfiguration } from "./RuntimeHostConfiguration";
import type { RuntimeModule } from "./RuntimeModule";
import type { RuntimeModuleSnapshot } from "./RuntimeModuleSnapshot";
export declare class RuntimeHost implements ApplicationHost, AsyncDisposable {
    readonly application: Application;
    readonly events: EventBus;
    readonly services: ServiceContainer;
    private stateInternal;
    private readonly modulesInternal;
    private readonly moduleSnapshotsInternal;
    private readonly initializedModuleIds;
    private readonly moduleResolver;
    private readonly configurationValidator;
    private publishedHealth?;
    constructor(configuration: RuntimeHostConfiguration);
    constructor(application: Application, events?: EventBus, services?: ServiceContainer);
    get state(): LifecycleState;
    get modules(): readonly RuntimeModule[];
    get moduleDiagnostics(): readonly RuntimeModuleSnapshot[];
    get health(): RuntimeHealthReport;
    get diagnostics(): DiagnosticReport;
    registerModule(runtimeModule: RuntimeModule): void;
    start(): Promise<void>;
    stop(): Promise<void>;
    restart(): Promise<void>;
    dispose(): Promise<void>;
    private initialize;
    private validateConfiguration;
    private activateModules;
    private resolveModules;
    private shutdownModules;
    private publish;
    private publishDiagnosticChangeIfNeeded;
    private publishModuleInitialized;
    private publishModuleEvent;
    private isStoppableModule;
    private isDisposableModule;
    private updateModuleSnapshot;
    private errorMessage;
    private toModuleHealthReport;
    private moduleHealth;
    private aggregateHealth;
    private applicationVersion;
    private toDiagnosticIssue;
    private toConfiguration;
    private ensureAvailable;
}
